# タグ階層展開・タグ構成類似度スコアリング機能 実装指示書

- 文書番号: IMPL-202608261450
- 対象プロジェクト: chatbot_invitro
- 参照文書:
  - 要件定義書 `docs/requirement/202608261450_タグ階層展開・タグ構成類似度スコアリング機能要件定義書.md`（REQ-202608261450）
  - ADR `docs/adr/0058-knowledge-mcp-tag-ancestor-expansion.md`、`docs/adr/0059-knowledge-mcp-tag-similarity-scoring.md`（本機能のために新設）
  - 関連（既存）: ADR-0005（タグの関連テーブル化）、ADR-0009・0010（`select_tags`）、ADR-0057（`agent_invitro`の会話タグ検索反映方式）
  - 既存実装（変更対象）: `knowledge_mcp/src/knowledge_mcp/repository/qa_repository.py`、`knowledge_mcp/src/knowledge_mcp/mcp/server.py`、`docker-compose.yml`、`terraform/main/app/main.tf`
- 対象コンポーネント: `knowledge_mcp`のみ。`tag_selector_mcp`・`agent_invitro`・`web_backend`のコードには変更を加えない（要件定義書10章）。
- 本書の位置づけ: 要件定義書・ADR-0058・ADR-0059で確定した方針を、実装担当者が着手できる粒度まで具体化した指示書である。本書はコードを仕様として提示するが、実際の`.py`/`.yml`/`.tf`ファイルへの反映は別途担当者が本書の指示に従って行うこと。

## 0. 本書作成にあたり確定した事項

| 項目 | 決定内容 | 根拠・備考 |
|---|---|---|
| タグ閉包・タグ類似度の計算場所 | SQL側で祖先タグ閉包（`WITH RECURSIVE`）とベクトル近傍候補プールの取得までを行い、Jaccard係数・重み付き合成はPython側（`QARepository.search`）で行う | **本書での確定事項**。既存の`distance_to_score`もPython側で計算しており、既存の実装方針と揃える |
| `tags`引数の名前解決に失敗したタグの扱い | `tag`テーブルに存在しない名前は`WHERE name = ANY(%(tag_names)s)`の時点で単純に該当なしとなり、暗黙に無視される（要件定義書6.1節で言及した「無視する」という挙動は、SQLの`WHERE`句が自然に満たす。個別の分岐コードは不要） | **本書での確認事項** |
| `TAG_SIMILARITY_WEIGHT`・`SEARCH_CANDIDATE_POOL_SIZE`の注入方法 | 環境変数は`mcp/server.py`の`create_server()`でのみ読み取り、`QARepository`のコンストラクタ引数として渡す（既存の`default_top_k`と同様、`SearchService`やMCPツール引数は経由しない） | **本書での確定事項**。`search_knowledge`のMCPツール入力スキーマ（`query`/`top_k`/`tags`/`category`/`min_score`）は変更しないため（要件定義書4.1節） |
| `SEARCH_CANDIDATE_POOL_SIZE`の`top_k`依存既定値の実装方法 | 環境変数が未設定の場合は`None`とし、`QARepository.search`内で呼び出しごとに`max(top_k * 10, 50)`を算出する（環境変数はこの計算式自体を上書きする固定値としてのみ使う） | **本書での確認事項**。要件定義書6.4節の既定値をそのまま実装に落とし込む |
| 既存の`tags`引数のAND条件の削除範囲 | `QARepository.search`の`conditions`（`WHERE`句によるハードフィルタ）からタグ条件を削除し、`category`条件のみを`WHERE`句に残す。タグは候補プール取得後のスコアリングでのみ扱う | **本書での確認事項**。5.1節のコード参照 |
| `metadata`への内訳追加のキー名 | `embedding_score`・`tag_similarity`・`tag_similarity_weight`（要件定義書6.3節のJSON例と同じ）。`tags`が未指定の場合、`tag_similarity`・`tag_similarity_weight`は`metadata`に含めない（`None`を入れるのではなくキー自体を省略する） | **本書での確認事項** |

## 1. 全体進行フェーズ

| Phase | 目的 | 主な成果物 | 前提 |
|---|---|---|---|
| Phase 1 | `knowledge_mcp`: `QARepository.search`の書き換え | タグ閉包・候補プール・総合スコアを実装したSQL・Python | なし |
| Phase 2 | `knowledge_mcp`: 環境変数の配線 | `mcp/server.py`の`create_server()`への引数追加 | Phase 1（コンストラクタ引数名の確定後） |
| Phase 3 | ローカル`docker-compose`環境 | `docker-compose.yml`の`knowledge_mcp`サービスへの環境変数追加 | Phase 2 |
| Phase 4 | AWS環境（Terraform） | `terraform/main/app/main.tf`の`module "knowledge_mcp"`への環境変数追加 | Phase 2 |
| Phase 5 | 検証・受け入れ | 6章のテスト結果、8章DoDチェック結果 | Phase 1〜4完了 |

## 2. WBS（作業分解構成）

| No. | タスク | 参照 | 成果物 |
|---|---|---|---|
| T1 | `QARepository.search`の書き換え実装指示（タグ閉包CTE、候補プール取得、Jaccard類似度・総合スコア算出、`metadata`内訳追加） | ADR-0058、ADR-0059、5.1節 | `knowledge_mcp/src/knowledge_mcp/repository/qa_repository.py`への変更差分 |
| T2 | `mcp/server.py`の`create_server()`への環境変数読み取り・`QARepository`コンストラクタ引数追加実装指示 | 5.2節 | `knowledge_mcp/src/knowledge_mcp/mcp/server.py`への変更差分 |
| T3 | `docker-compose.yml`の`knowledge_mcp`サービスへの環境変数追加実装指示 | 5.3節 | `docker-compose.yml`への変更差分 |
| T4 | `terraform/main/app/main.tf`の`module "knowledge_mcp"`への環境変数追加実装指示 | 5.4節 | `terraform/main/app/main.tf`への変更差分 |
| T5 | ローカル環境での動作確認（6.1節） | 6章 | 確認結果 |
| T6 | AWS環境での動作確認（6.2節） | 6章 | 確認結果 |
| T7 | 既存呼び出し元（`agent_invitro`・検証機能）への非影響確認（6.3節） | 6章、10章 | 確認結果 |
| T8 | 性能計測（候補プール件数・重みの暫定値の妥当性確認、6.4節） | 6章、7章 Open Issue #2・#3 | 確認結果 |
| T9 | DoDチェックリスト実施 | 8章 | チェック結果 |

## 3. ディレクトリ・モジュール構成（変更差分）

```
knowledge_mcp/
└── src/knowledge_mcp/
    ├── repository/
    │   └── qa_repository.py     … search() を書き換え（T1）。distance_to_score は変更なし
    └── mcp/
        └── server.py             … create_server() に環境変数読み取り・引数注入を追加（T2）

docker-compose.yml                … knowledge_mcp サービスの environment に追加（T3）
terraform/main/app/main.tf         … module "knowledge_mcp" の environment に追加（T4）
```

既存の`knowledge_mcp/src/knowledge_mcp/services/search_service.py`・`mcp/tools.py`・`mcp/schemas.py`・`models/document.py`には変更を加えない。`search_knowledge`のMCPツール入出力スキーマの型定義（0章）は変更しない。

## 4. Phase 0: 事前確認事項

本機能は新規AWSリソース・新規ネットワーク経路を伴わない（`knowledge_mcp`のコンテナ・接続方式は変更しない、環境変数2件の追加のみ）。実装着手をブロックする外部確認事項はない。ただし、7章Open Issue #4（想定QA件数・タグ件数の規模）は、T8（性能計測）の前提として、実装着手後の早い段階で確認しておくことを推奨する。

## 5. リソース仕様

### 5.1 `QARepository.search`の書き換え（T1）

既存の`knowledge_mcp/src/knowledge_mcp/repository/qa_repository.py`は、`tags`引数をAND条件の`WHERE`句として組み立て、`ORDER BY <=> LIMIT top_k`で確定件数を取得していた。これを次のように書き換える。`distance_to_score`関数・`SOURCE_TYPE`定数・クラス冒頭のdocstringは変更しない。

```python
"""QARepository（実装指示書 5.3 / 要件6.3、IMPL-202608261450 T1 で祖先タグ展開・
タグ構成類似度スコアリングに対応）。

既存 app/src/db.py の search_similar を移植・拡張し、title / category / tags を併せて取得する。
既存 search_similar 同様、question_altered 単位（1 altered question = 1 行）でベクトル近傍を取り、
qa_original / category / tag を結合して Document へ変換する。

IMPL-202608261450 により、tags 引数は「AND完全一致のハードフィルタ」から「タグ構成類似度
（祖先タグを含めたJaccard係数）と埋め込み類似度を重み付き合成した総合スコアへ反映するソフトな
シグナル」へ変更された（ADR-0058・ADR-0059）。
"""

from __future__ import annotations

from typing import Callable, List, Optional

from ..db.connection import Database, to_vector_str
from ..models.document import Document

SOURCE_TYPE = "qa"


def distance_to_score(distance: float) -> float:
    """スコア変換式（実装指示書 0節）: score = 1 / (1 + distance)。

    distance は pgvector の <=> 値（小さいほど類似）。暫定式で、精度検証・調整の対象。
    """
    return 1.0 / (1.0 + distance)


def _jaccard(a: set, b: set) -> float:
    """空集合同士は 0.0（要件定義書6.2節: 両方空でも重なりなし扱い。呼び出し元で
    「タグ未指定」自体は別途分岐しており、ここに来るのは常に入力側が非空のケースのみ）。"""
    union = a | b
    if not union:
        return 0.0
    return len(a & b) / len(union)


class QARepository:
    def __init__(
        self,
        db: Database,
        embed_fn: Callable[[str], List[float]],
        tag_similarity_weight: float = 0.5,
        candidate_pool_size: Optional[int] = None,
    ):
        """
        db: DB接続ファクトリ（都度コネクションを生成する）。
        embed_fn: クエリ文字列を受け取りembeddingベクトルを返す関数（DI）。
        tag_similarity_weight: 総合スコアにおけるタグ類似度の重み w（0.0〜1.0）。
            環境変数 TAG_SIMILARITY_WEIGHT（既定 0.5）。ADR-0059。
        candidate_pool_size: 候補プールの固定件数。None の場合は呼び出しごとに
            max(top_k * 10, 50) を用いる（環境変数 SEARCH_CANDIDATE_POOL_SIZE 未設定時の既定挙動）。
        """
        self.db = db
        self.embed_fn = embed_fn
        self.tag_similarity_weight = tag_similarity_weight
        self.candidate_pool_size = candidate_pool_size

    def search(
        self,
        query: str,
        tags: Optional[List[str]] = None,
        category: Optional[str] = None,
        top_k: int = 5,
    ) -> List[Document]:
        embedding = self.embed_fn(query)
        vec_str = to_vector_str(embedding)
        pool_size = self.candidate_pool_size or max(top_k * 10, 50)

        conditions: list[str] = []
        params: dict = {"query_vec": vec_str, "pool_size": pool_size}

        if category:
            conditions.append(
                "qa_original.category_id = (SELECT id FROM category WHERE name = %(category)s)"
            )
            params["category"] = category

        where_clause = ("WHERE " + " AND ".join(conditions)) if conditions else ""

        # tags が空/未指定の場合、入力タグ閉包は計算しない（後段で combined_score = embedding_score
        # とする分岐に使う。要件定義書6.2節）。
        tag_names = list(dict.fromkeys(tags)) if tags else []
        params["tag_names"] = tag_names or [None]  # ANY(%(tag_names)s) が空配列と None を区別しないよう [None] を渡す

        sql = """
            WITH RECURSIVE tag_closure(tag_id, closure_id) AS (
                SELECT id, id FROM tag
                UNION ALL
                SELECT tc.tag_id, t.parent_tag_id
                FROM tag_closure tc
                JOIN tag t ON t.id = tc.closure_id
                WHERE t.parent_tag_id IS NOT NULL
            ),
            input_closure AS (
                SELECT COALESCE(array_agg(DISTINCT tc.closure_id), ARRAY[]::integer[]) AS closure
                FROM tag_closure tc
                WHERE tc.tag_id IN (SELECT id FROM tag WHERE name = ANY(%(tag_names)s))
            )
            SELECT
                qa_original.uuid AS qa_id,
                qa_original.title AS title,
                qa_original.answer_text AS answer,
                category.name AS category_name,
                COALESCE(
                    (SELECT array_agg(tag.name) FROM qa_tag JOIN tag ON tag.id = qa_tag.tag_id
                     WHERE qa_tag.qa_id = qa_original.uuid),
                    ARRAY[]::text[]
                ) AS tag_names,
                COALESCE(
                    (SELECT array_agg(DISTINCT tc.closure_id)
                     FROM qa_tag qt JOIN tag_closure tc ON tc.tag_id = qt.tag_id
                     WHERE qt.qa_id = qa_original.uuid),
                    ARRAY[]::integer[]
                ) AS qa_closure,
                (SELECT closure FROM input_closure) AS input_closure,
                question_altered.embedding <=> %(query_vec)s::vector AS distance
            FROM question_altered
            LEFT JOIN qa_original ON question_altered.qa_id = qa_original.uuid
            LEFT JOIN category ON qa_original.category_id = category.id
            {where_clause}
            ORDER BY question_altered.embedding <=> %(query_vec)s::vector
            LIMIT %(pool_size)s
        """.format(where_clause=where_clause)

        with self.db.cursor() as cur:
            cur.execute(sql, params)
            rows = cur.fetchall()

        input_closure_set: set = set(rows[0][6]) if rows and rows[0][6] else set()
        has_tags = bool(tag_names)

        scored: List[tuple[float, Document]] = []
        for row in rows:
            (
                qa_id, title, answer, category_name,
                tag_names_row, qa_closure, _input_closure, distance,
            ) = row

            embedding_score = distance_to_score(float(distance))
            metadata = {
                "tags": list(tag_names_row) if tag_names_row else [],
                "category": category_name,
                "guid": qa_id,
            }

            if has_tags:
                tag_similarity = _jaccard(input_closure_set, set(qa_closure or []))
                w = self.tag_similarity_weight
                combined_score = (1 - w) * embedding_score + w * tag_similarity
                metadata["embedding_score"] = embedding_score
                metadata["tag_similarity"] = tag_similarity
                metadata["tag_similarity_weight"] = w
            else:
                combined_score = embedding_score

            scored.append((
                combined_score,
                Document(
                    id=qa_id,
                    source_type=SOURCE_TYPE,
                    title=title or "",
                    content=answer or "",
                    score=combined_score,
                    metadata=metadata,
                ),
            ))

        scored.sort(key=lambda pair: pair[0], reverse=True)
        return [doc for _, doc in scored[:top_k]]
```

`SearchService.search`（`services/search_service.py`）は、この`QARepository.search`が返す`Document`一覧を集約し降順ソートするだけの既存ロジックのままでよい（`Document.score`が既に総合スコアになっているため、`SearchService`側の変更は不要）。`mcp/tools.py`の`search_knowledge`ハンドラの`min_score`フィルタ（`doc.score >= min_score`）も、`doc.score`が総合スコアに変わるだけで、コード自体は変更不要である（0章）。

### 5.2 `mcp/server.py`の環境変数配線（T2）

既存の`create_server()`に、次の2行を追加する（`default_top_k`の読み取りと同じ場所）。

```python
def create_server() -> FastMCP:
    database_url = os.environ["DATABASE_URL"]
    embedding_provider = os.environ.get("EMBEDDING_PROVIDER", "lmstudio")
    default_top_k = int(os.environ.get("KNOWLEDGE_MCP_DEFAULT_TOP_K", "5"))
    port = int(os.environ.get("KNOWLEDGE_MCP_PORT", "8100"))
    # IMPL-202608261450 T2: タグ構成類似度スコアリング（ADR-0058・ADR-0059）
    tag_similarity_weight = float(os.environ.get("TAG_SIMILARITY_WEIGHT", "0.5"))
    _pool_size_env = os.environ.get("SEARCH_CANDIDATE_POOL_SIZE")
    candidate_pool_size = int(_pool_size_env) if _pool_size_env else None

    ...

    qa_repository = QARepository(
        db,
        embed_fn,
        tag_similarity_weight=tag_similarity_weight,
        candidate_pool_size=candidate_pool_size,
    )
    tag_repository = TagRepository(db)
    qa_management_repository = QaManagementRepository(db, embed_fn)
    search_service = SearchService([qa_repository])
```

`register_tools()`の呼び出し・`mcp/tools.py`自体には変更を加えない（`QARepository`はコンストラクタでのみ新しい引数を受け取り、`search()`の呼び出しシグネチャ自体は変わらないため）。

### 5.3 `docker-compose.yml`（T3）

`knowledge_mcp`サービスの`environment`に追加する（既存の`KNOWLEDGE_MCP_DEFAULT_TOP_K`と同じ並び）。

```yaml
  knowledge_mcp:
    environment:
      # 既存の環境変数はそのまま
      KNOWLEDGE_MCP_DEFAULT_TOP_K: ${KNOWLEDGE_MCP_DEFAULT_TOP_K:-5}
      # IMPL-202608261450: タグ階層展開・タグ構成類似度スコアリング（ADR-0058・ADR-0059）
      TAG_SIMILARITY_WEIGHT: ${TAG_SIMILARITY_WEIGHT:-0.5}
      SEARCH_CANDIDATE_POOL_SIZE: ${SEARCH_CANDIDATE_POOL_SIZE:-}
```

`SEARCH_CANDIDATE_POOL_SIZE`は既定で未設定（空文字）のままとし、`QARepository`側の`max(top_k * 10, 50)`計算に委ねる（5.1節）。

### 5.4 Terraform（T4）

`terraform/main/app/main.tf`の`module "knowledge_mcp"`の`environment`ブロック（既存の`KNOWLEDGE_MCP_DEFAULT_TOP_K = "5"`の行の下）に追加する。

```hcl
module "knowledge_mcp" {
  # ...既存の設定はそのまま...
  environment = {
    EMBEDDING_PROVIDER          = "bedrock"
    BEDROCK_EMBEDDING_MODEL_ID  = var.bedrock_embedding_model_id
    BEDROCK_REGION               = var.aws_region
    KNOWLEDGE_MCP_DEFAULT_TOP_K = "5"
    # IMPL-202608261450: タグ階層展開・タグ構成類似度スコアリング（ADR-0058・ADR-0059）
    TAG_SIMILARITY_WEIGHT       = "0.5"
    # SEARCH_CANDIDATE_POOL_SIZE は未設定のままとし、アプリケーション側の既定計算式に委ねる
    # （5.1節、max(top_k * 10, 50)）。値を固定したくなった場合のみ追加する。
  }
  # ...以下変更なし...
}
```

新規セキュリティグループルール・新規Terraformリソースは発生しない（`knowledge_mcp`のネットワーク到達性・コンピュート構成は変更しない）。

## 6. テスト・検証指示

### 6.1 ローカル`docker-compose`環境での動作確認（T5）

- 親子関係を持つタグ（既存のタグ階層データ、または`TagTreePage`から一時的に作成した親子タグ）を用意する。
- 子タグ名のみを`tags`引数に指定して`search_knowledge`を呼び出し（MCP Inspector等）、その祖先（親タグ）にのみ直接紐づくQAが結果に含まれることを確認する（`results[].metadata.tag_similarity`が0より大きいこと）。
- 逆に親タグを`tags`引数に指定し、子タグにのみ直接紐づくQAが結果に含まれることを確認する。
- `tags`を指定しない呼び出しで、`results[].score`が本機能の変更前（`1 / (1 + distance)`のみ）と一致することを確認する（後方互換性、要件定義書8章）。

### 6.2 AWS環境での動作確認（T6）

- `terraform apply`後、`admin_ui`経由の検証機能（`/admin/verification`）で既存タグ・新規タグを使った検索を行い、結果が返ることを確認する（`web_backend`側の変更は不要だが、`knowledge_mcp`の応答形式・値が変わるため、通しで確認する）。

### 6.3 既存呼び出し元への非影響確認（T7）

- `agent_invitro`のチャット生成（`/ask-sl`）が、コード変更なしに動作し続けることを確認する（`search_knowledge`の入出力スキーマは変更していないため）。
- 検証機能（既存タグ入力拡張前の状態、または`docs/requirement/202608261510_...`実装後の状態）からの呼び出しが、エラーなく動作することを確認する。

### 6.4 性能計測（T8）

- 実データ（または実データに近い件数のテストデータ）で、`tags`指定あり・なしそれぞれの`search_knowledge`応答時間を計測する。タグ階層が深い場合の`WITH RECURSIVE`のコストを確認する。
- 計測結果をもとに、`TAG_SIMILARITY_WEIGHT`・`SEARCH_CANDIDATE_POOL_SIZE`の既定値の妥当性を判断する（7章 Open Issue #2・#3）。

## 7. Open Issues（実装時に確定が必要な事項）

要件定義書12章のOpen Issueのうち、実装フェーズで扱うべき事項を再掲する。

1. **`agent_invitro`側ワークアラウンド（ADR-0057）の見直し要否**（要件定義書12章#1）: 本書の実装完了後、`agent_invitro/src/tags.py`の`search_with_merged_tags`（タグ別複数回呼び出し＋統合）を単純化するかどうかは、本書のスコープ外であり別途判断する。
2. **`TAG_SIMILARITY_WEIGHT`の適正値**（要件定義書12章#2、本書6.4節T8）: 実データでの検索精度評価を通じて調整する。
3. **`SEARCH_CANDIDATE_POOL_SIZE`の適正値**（要件定義書12章#3、本書6.4節T8）: 同上。
4. **想定QA件数・タグ件数の規模**（要件定義書12章#4）: T8の性能計測の前提として、発注者に確認する必要がある。
5. **評価スクリプト（`evaluate.py`）との整合**（要件定義書12章#5）: 本書のスコープ外。

## 8. 完了条件（要件定義書11章のDoDに対応する実装レベルの確認項目）

- [ ] 子タグのみを`tags`引数に指定した場合、その祖先にのみ紐づくQAも検索結果に含まれる（6.1節）。
- [ ] 親タグを`tags`引数に指定した場合、その子タグにのみ紐づくQAも検索結果に含まれる（6.1節）。
- [ ] `tags`を指定しない呼び出しの結果（順位・`score`）が変更前と一致する（6.1節）。
- [ ] `results[].metadata`に`embedding_score`・`tag_similarity`・`tag_similarity_weight`が、`tags`指定時のみ含まれる（5.1節）。
- [ ] `agent_invitro`・検証機能が、コード変更なしに動作し続ける（6.3節）。
- [ ] ローカル`docker-compose`環境・AWS環境の両方で動作する（6.1・6.2節）。

## 9. 実装時の注意点・落とし穴

- **`tags=[None]`のANY比較**: PostgreSQLの`= ANY(array)`は、`array`が空配列（`{}`）の場合`WHERE ... = ANY('{}')`は常に偽になり、`tag_names`が空のときに`input_closure`が正しく空集合になる。本書のコード（5.1節）では`tag_names or [None]`としているが、`ANY(ARRAY[NULL])`は`NULL`との比較になり常に偽（`NULL`は何とも一致しない）となるため、結果的に`input_closure`は空になり意図通りである。ただし、この挙動は実装時に必ずユニットテストで確認すること（`tags=[]`のとき`input_closure`が空集合になり、`has_tags`分岐で`combined_score = embedding_score`になることを確認する）。
- **`vector`型キャスト**: 既存コードの`%s`のみのプレースホルダ（位置引数）から、本書のコードは`%(name)s`（名前付きプレースホルダ、`dict`パラメータ）に変更している。`psycopg2`は同一クエリ内で位置引数と名前付き引数を混在できないため、`cur.execute(sql, params)`の`params`は必ず`dict`で統一すること。既存の`to_vector_str`が返す文字列を`::vector`へ明示キャストする点も、既存コードの挙動（暗黙キャスト）と変わらないことを確認する。
- **候補プールの外側にあるQAの扱い**: 候補プール（`LIMIT pool_size`）に入らなかったQAは、タグ類似度がどれだけ高くても結果に現れない（要件定義書6.4節・8章）。これは既知の制約であり、バグではない。
- **`tag_closure`の計算コスト**: `WITH RECURSIVE tag_closure`は`tag`テーブル全件を起点に毎回計算する。`tag`件数が多い場合、`tag.parent_tag_id`へのインデックス（既存の外部キー制約により通常は自動的に近い効果を持つインデックスが張られるが、明示的な`CREATE INDEX`の要否を実装時に確認すること）。

## 10. 実装着手前に確認をお願いしたい事項（再掲）

0章の内容（環境変数の注入方法、`SEARCH_CANDIDATE_POOL_SIZE`の既定値計算式、AND条件削除の範囲、`metadata`キー名）は、要件定義書REQ-202608261450・ADR-0058・ADR-0059として既に確定・Accepted済みの方針を実装レベルで具体化したものである。4章の通り、本機能の実装着手をブロックする外部確認事項はない。
